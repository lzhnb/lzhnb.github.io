{"meta":{"title":"氵景页の博客","subtitle":"码路上","description":"苦逼的日子也不乏有趣的时刻","author":"氵景页","url":"https://lzhnb.github.io","root":"/"},"pages":[{"title":"文章分类-Categories","date":"2019-04-24T07:38:48.000Z","updated":"2019-04-24T07:40:29.195Z","comments":true,"path":"categories/index.html","permalink":"https://lzhnb.github.io/categories/index.html","excerpt":"","text":""},{"title":"标签-Tags","date":"2019-04-24T07:41:40.000Z","updated":"2019-04-24T07:42:20.693Z","comments":true,"path":"tags/index.html","permalink":"https://lzhnb.github.io/tags/index.html","excerpt":"","text":""}],"posts":[{"title":"CANet论文解读","slug":"CANet解读","date":"2019-05-01T05:50:47.000Z","updated":"2019-05-02T04:27:31.627Z","comments":true,"path":"category/2019/05/01/CANet解读.html","link":"","permalink":"https://lzhnb.github.io/category/2019/05/01/CANet解读.html","excerpt":"","text":"CANet: Class-Agnostic Segmentation Networks with Iterative Refinement and Attentive Few-Shot Learning论文解读（施工中）关于小样本学习详情见Matching_Networks_for_One_Shot_Learning论文解读 CANet 介绍简介针对语义分割（Semantic Segementation）的问题，以往传统的方法都是针对目标对象建立分割数据集用于训练，这样的方法所需要的 pixel-level 数据集的标注成本极高，且该方法仅适用于训练过的类别对象，无法用于未知（没有参与训练）类别的物体的分割任务。虽然之前已经有了利用小样本学习的方法进行语义分割的先例（OSLSM），但是效果不尽人意，而且与传统方法的 state-of-art 相比任有较大差距。这篇论文的核心在于提出了一种新的注意力机制（attention mechanism）能够更高效地融合 support_set 中的信息，实现更好的分割效果。最终在性能指标方面，取得了在 Pascal VOC2012 数据集上利用 1-shot 实现了 mAP 为55.4%，以及利用 5-shot 实现了 mAP 为 57.1% 的好成绩 网络结构 图示 解读启发于之前小样本学习的工作，针对两个输入 support_set 和 query_set 分别定义 support_branch 与 query_branch 用来提取所需的特征，然后利用 support_branch 提取到的特征对 query_branch 进行指导的这种结构也在 CANet 上继续沿用，dense comparison module(DCM) 便是实现文中所说的 ‘guide’ 的网络结构，然后再投入后面的递归环节利用 iterative optimation mudule(IOM) 实现语义分割。 启发小样本学习一开始应用于图像分类，其启发式的工作- metric learning 便是在提取二者特征向量后用一个距离函数来度量二者之间的相似性。但与分类不同，分割时一个 pixel-level 的工作，相当于将每个像素进行分类。作者受 metric learning 的启发，想将该方法移植到分割问题上，即对每个pixel都提取对应特征，然后对 support_set 与 query_set 的这些 ‘pixel-level-feature-vector’（博主自制，方便表达，简写为’pf’） 进行配对作为一个又一个的 pair， 然后对 pair 中的两个 pf 进行距离度量实现分类，进而实现分割。但是这种方法带来的问题是显而易见的，每张图上的像素点数量都是数以万计的，组合成 pair 的可能性更多，如果这样实现的话进行训练与测试的开销时不可想象的。针对这样的问题，作者将在 support_set 中提取一个全局代表（global representation，简称 gr） 然后用 gr 分别与query_set 中提取的 pf 进行度量在 pixel-level 上实现了 metric learning 的方法。 关于特征论文中对网络提取的特征进行了比较深入的研究。比较底层的特征有例如颜色与边缘这种公共的特征，组合成的比较高级特征就涉及到了物体级别的概念比如说类别，作者比较关心的是处于两者之间中间层的特征，这些特征可能就涉及到整个类别的某项比较鲜明的局部特征比如轮胎之于汽车。我们知道，小样本学习在测试时是处理没有”见过”的类别，作者认为有效地提取中间层地特征并合理地利用它对小样本学习能取得很好地效果，在实际操作中，作者在 DCM 环节中对多层的特征都做了比较操作。不过针对同样类别的物体，得到的图片也不尽相同，其提取出来的特征可能只有部分具有一致性。 实现思路所以，如果仅如上所说进行 comparison 是不足以分割出整个物体区域的（个人理解：comparison 仅能识别出具有一致性的特征所属区域），但是这一步能够实现对目标物体的定位。然后作者了解到，在半监督分割（semi-automatic segmentation）中，针对没有”见过”的对象给予弱标注（click or scribble annotations），该网络能够将其补全。启发于半监督分割的方法，作者在 DCM 环节后加入了一个递归环节，利用 IOM 结构实现对 DCM 所得的分割结果进行精修（refinement）。 优化 k-shot又说回之前提到的作者说的注意力机制，作者指出以往实现 k-shot 的方法都是基于 1-shot 进行的，而且它们都是用无需训练的方式对 support_set 的结果进行简单的融合，比如均值处理。 测试论文中说到为了减少标注所花费的精力，训练好的网络可以接受仅有 bounding box annotation 的图片作为 support_set，能够实现对 query_set 做到比较好的分类结果，与选择 pixel-level annotation 的图片作为 support_set 相比，其性能指标也比较有竞争力。具体如下图： 结构详解具体结构在图示中我们已经展示过了整个网络的大概图示，下面我们就具体地讲解一下整个网络地结构，结构如下图： DCM首先是 DCM 环节，结构如下图： 我们可以看到 DCM 的 backbone 是 resnet，在特征提取方面，support_set 与 query_set 的网络参数是共享的，在特征方面上作者说利用中间层的特征会取得较好的效果，所以提取的特征部分仅取 resnet 中 block2 与 block3 的输出进行拼接作为图中黄色框区域的输出，然后作后续处理 （这里原文的表述是 block2 的输出尺寸为原图的 1/8，直接将 block2 与 block3 拼接起来做后续的处理，但是博主所了解到的在 block3 中的第一层卷积层的 stride 为2就会实现下采样，且 block2 尺寸应为原图的 1/4，估计是block3的下采样层为 block2 中最后一层，block3 并不实现下采样） 对输出的特征投进一个 256 维 33的 卷积进行得到一个尺寸维原图尺寸 1/8 大小，channels 为 256 的 feature map。然后将 mask 等效缩放到原图的 1/8大小然后与 support_set 的 feature map 对应做点积，得到一个对应 foreground 区域为原值，对应 background 区域为 0 的一个 feature map 然后做一个全局累加池化（global sum pooling）再上采样成 feature map 大小（该操作等效于对应图中点积操作之后的 feature map 中每个 vector 的值做一个均值操作）然后与经过 256 维 33 卷积的 query_set 对应的 feature map 做一个拼接再过，然后再经过一个 256 维 3*3 的卷积得到 DCM 的最终输出（途中蓝色方块所示）。 IOM接下来是 IOM 环节，结构如下图： 我们可以看到这是一个递归的结构。在之前提到过如果仅靠 DCM 并不能十分准确地对物体实现分割，但是能够提供十分关键的定位信息以及比较粗糙的分割结果，再 ICM 这个环节中就是要实现一个精修的效果。论文中给出了这样的一个公式： M_t=x+F(x,y_{t-1})对于第 $t$ 次的递归，$x$ 是 DCM 输出的 feature map 一直没有变，$M_t$ 为第 $t$ 次递归的输出，不过该输出指的是第一个相交点出的输出，$y_{t-1}$ 则是第 $t-1$ 次递归输出的 mask 结果就是最后的那个二值输出（注意 $M_t$ 与 $y_t$ 实际并不相同），$F(\\cdot)$ 则是指将 $x$ 与 $y_{t-1}$ 进行拼接然后经过两个 256 维 33 卷积的操作。如果要得到 $y_t$ 也就是第 $t$ 次递归输出的 mask 的话则还要将 $M_t$ 投入两个残差块然后经过一个 ASPP(Atrous Spatial Pyramid Pooling module) 环节，在这个环节中对输入同时进行四个分支的操作，分别是三个 256维 33 的空洞卷积(atrous convolution)，空洞率分别维 6, 12, 18，以及一个 256维 11 的卷积然后进行拼接再经过一个 256维 11 的卷积，最后再经过一个 2维 1*1 的卷积得到关于关于 foreground 与 background 的 mask， 然后对每个点利用 softmax 函数来进行置信度评估，最终输出的就是 confidence map，也就是第 $t$ 次递归的输出 $y_t$。对于某次的输出 $y_n$ 也有概率 $p_r$ 使其全部清零作为下一次递归的输入，这种机制类似于 dropout 机制，且能够实现 end-to-end 的训练。 注意力机制前面提到过作者针对 k-shot 的情况提出了和以往不同的 k-shot 处理方式。作者提出了一种注意力机制能够高效地利用和融合 support_set 中获取的特征信息，结构如下图： 在 k-shot 的情况下，关键在于 DCM 部分的改变，与上面所讲的不同，关键在于后面对拼接后的 feature map 进行两个分支的操作，上面的第一个分支与 DCM 后面的操作无异关键在于下面的第二个分支文中称为 attention branch 。我们从图中可以看到 attention branch 中经过两个 block 得到一个参数-$\\hat{\\lambda}_k$。第一个 block 代表的操作为一个 256维 33 的卷积以及一个 33 的 max pooling 操作，第二个 block 代表的操作为一个 3*3 的卷积以及一个 全局均值池化（global average pooling）就得参数 $\\lambda$，这样 k-shot 就能得到 k 个 $\\lambda$ 值，利用如下 softmax 公式： \\hat{\\lambda}_k=\\frac{e^{\\lambda_i}}{\\sum_{j=1}^{k}e^{\\lambda_j}}这样我们就能得到参数 $e^{\\lambda_i}$ 然后与第一个 branch 得到 feature map 进行点积再进行后续的操作。","categories":[{"name":"论文笔记","slug":"论文笔记","permalink":"https://lzhnb.github.io/categories/论文笔记/"}],"tags":[{"name":"小样本学习","slug":"小样本学习","permalink":"https://lzhnb.github.io/tags/小样本学习/"},{"name":"语义分割","slug":"语义分割","permalink":"https://lzhnb.github.io/tags/语义分割/"}]},{"title":"ImageDataGenerator源码阅读","slug":"ImageDataGenerator源码阅读","date":"2019-04-28T12:51:09.000Z","updated":"2019-05-03T08:19:05.693Z","comments":true,"path":"category/2019/04/28/ImageDataGenerator源码阅读.html","link":"","permalink":"https://lzhnb.github.io/category/2019/04/28/ImageDataGenerator源码阅读.html","excerpt":"","text":"ImageDataGenerator源码阅读（施工中）介绍ImageDataGenerator是Keras中定义的一个类（函数）用于对数据库中的图像数据(raw-data)做处理，生成训练以及测试中所需的数据，在分类问题上基本都可以利用该函数解决如何生成训练数据的问题，是一个十分方便且功能强大的函数。 提供接口keras提供的ImageDataGenerator接口如下所示：12345678910111213141516171819202122keras.preprocessing.image.ImageDataGenerator(featurewise_center=False, samplewise_center = False, featurewise_std_normalization = False, samplewise_std_normalization = False, zca_whitening = False, zca_epsilon = 1e-06, rotation_range = 0, width_shift_range = 0.0, height_shift_range = 0.0, brightness_range = None, shear_range = 0.0, zoom_range = 0.0, channel_shift_range = 0.0, fill_mode = 'nearest', cval = 0.0, horizontal_flip = False, vertical_flip = False, rescale = None, preprocessing_function = None, data_format = None, validation_split = 0.0, dtype = None) 相关参数相关参数如下： featurewise_center: 布尔值。将输入数据的均值设置为 0，逐特征进行。 samplewise_center: 布尔值。将每个样本的均值设置为 0。 featurewise_std_normalization: Boolean. 布尔值。将输入除以数据标准差，逐特征进行。 samplewise_std_normalization: 布尔值。将每个输入除以其标准差。 zca_epsilon: ZCA 白化的 epsilon 值，默认为 1e-6。 zca_whitening: 布尔值。是否应用 ZCA 白化。 rotation_range: 整数。随机旋转的度数范围。 width_shift_range: 浮点数、一维数组或整数 float: 如果 =1，则为像素值。 1-D 数组: 数组中的随机元素。 int: 来自间隔 (-width_shift_range, +width_shift_range) 之间的整数个像素。 width_shift_range=2 时，可能值是整数 [-1, 0, +1]，与 width_shift_range=[-1, 0, +1] 相同；而 width_shift_range=1.0 时，可能值是 [-1.0, +1.0) 之间的浮点数。 height_shift_range: 浮点数、一维数组或整数 float: 如果 =1，则为像素值。 1-D array-like: 数组中的随机元素。 int: 来自间隔 (-height_shift_range, +height_shift_range) 之间的整数个像素。 height_shift_range=2 时，可能值是整数 [-1, 0, +1]，与 height_shift_range=[-1, 0, +1] 相同；而 height_shift_range=1.0 时，可能值是 [-1.0, +1.0) 之间的浮点数。 shear_range: 浮点数。剪切强度（以弧度逆时针方向剪切角度）。 zoom_range: 浮点数 或 [lower, upper]。随机缩放范围。如果是浮点数，[lower, upper] = [1-zoom_range, 1+zoom_range]。 channel_shift_range: 浮点数。随机通道转换的范围。 fill_mode: {“constant”, “nearest”, “reflect” or “wrap”} 之一。默认为 ‘nearest’。输入边界以外的点根据给定的模式填充： ‘constant’: kkkkkkkk|abcd|kkkkkkkk (cval=k) ‘nearest’: aaaaaaaa|abcd|dddddddd ‘reflect’: abcddcba|abcd|dcbaabcd ‘wrap’: abcdabcd|abcd|abcdabcd cval: 浮点数或整数。用于边界之外的点的值，当 fill_mode = &quot;constant&quot; 时。 horizontal_flip: 布尔值。随机水平翻转。 vertical_flip: 布尔值。随机垂直翻转。 rescale: 重缩放因子。默认为 None。如果是 None 或 0，不进行缩放，否则将数据乘以所提供的值（在应用任何其他转换之前）。 preprocessing_function: 应用于每个输入的函数。这个函数会在任何其他改变之前运行。这个函数需要一个参数：一张图像（秩为 3 的 Numpy 张量），并且应该输出一个同尺寸的 Numpy 张量。 data_format: 图像数据格式，{“channels_first”, “channels_last”} 之一。”channels_last” 模式表示图像输入尺寸应该为 (samples, height, width, channels)，”channels_first” 模式表示输入尺寸应该为 (samples, channels, height, width)。默认为 在 Keras 配置文件 ~/.keras/keras.json 中的 image_data_format 值。如果你从未设置它，那它就是 “channels_last”。 validation_split: 浮点数。Float. 保留用于验证的图像的比例（严格在0和1之间）。 dtype: 生成数组使用的数据类型。部分参数说明 中心化操作-标准的减均值除标准差的操作 featurewise_center samplewise_center featurewise_std_normalization samplewise_std_normalization 归一化操作-rescale：8bit的图片缩放因子设为1.0/255实现归一化 数据增强-旋转翻折等操作 rotation_range width_shift_range width_shift_range shear_range horizontal_flip vertical_flip 源码源码讲解源码在这里我们将分段讲解该函数如下： init 部分123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105106107108109110111112113114115116117118119class ImageDataGenerator(object): \"\"\"Generate batches of tensor image data with real-time data augmentation. The data will be looped over (in batches). # Arguments Pass \"\"\" def __init__(self, featurewise_center=False, samplewise_center=False, featurewise_std_normalization=False, samplewise_std_normalization=False, zca_whitening=False, zca_epsilon=1e-6, rotation_range=0, width_shift_range=0., height_shift_range=0., brightness_range=None, shear_range=0., zoom_range=0., channel_shift_range=0., fill_mode='nearest', cval=0., horizontal_flip=False, vertical_flip=False, rescale=None, preprocessing_function=None, data_format='channels_last', validation_split=0.0, interpolation_order=1, dtype='float32'): self.featurewise_center = featurewise_center self.samplewise_center = samplewise_center self.featurewise_std_normalization = featurewise_std_normalization self.samplewise_std_normalization = samplewise_std_normalization self.zca_whitening = zca_whitening self.zca_epsilon = zca_epsilon self.rotation_range = rotation_range self.width_shift_range = width_shift_range self.height_shift_range = height_shift_range self.shear_range = shear_range self.zoom_range = zoom_range self.channel_shift_range = channel_shift_range self.fill_mode = fill_mode self.cval = cval self.horizontal_flip = horizontal_flip self.vertical_flip = vertical_flip self.rescale = rescale self.preprocessing_function = preprocessing_function self.dtype = dtype self.interpolation_order = interpolation_order if data_format not in &#123;'channels_last', 'channels_first'&#125;: raise ValueError( '`data_format` should be `\"channels_last\"` ' '(channel after row and column) or ' '`\"channels_first\"` (channel before row and column). ' 'Received: %s' % data_format) self.data_format = data_format if data_format == 'channels_first': self.channel_axis = 1 self.row_axis = 2 self.col_axis = 3 if data_format == 'channels_last': self.channel_axis = 3 self.row_axis = 1 self.col_axis = 2 if validation_split and not 0 &lt; validation_split &lt; 1: raise ValueError( '`validation_split` must be strictly between 0 and 1. ' ' Received: %s' % validation_split) self._validation_split = validation_split self.mean = None self.std = None self.principal_components = None if np.isscalar(zoom_range): self.zoom_range = [1 - zoom_range, 1 + zoom_range] elif len(zoom_range) == 2: self.zoom_range = [zoom_range[0], zoom_range[1]] else: raise ValueError('`zoom_range` should be a float or ' 'a tuple or list of two floats. ' 'Received: %s' % (zoom_range,)) if zca_whitening: if not featurewise_center: self.featurewise_center = True warnings.warn('This ImageDataGenerator specifies ' '`zca_whitening`, which overrides ' 'setting of `featurewise_center`.') if featurewise_std_normalization: self.featurewise_std_normalization = False warnings.warn('This ImageDataGenerator specifies ' '`zca_whitening` ' 'which overrides setting of' '`featurewise_std_normalization`.') if featurewise_std_normalization: if not featurewise_center: self.featurewise_center = True warnings.warn('This ImageDataGenerator specifies ' '`featurewise_std_normalization`, ' 'which overrides setting of ' '`featurewise_center`.') if samplewise_std_normalization: if not samplewise_center: self.samplewise_center = True warnings.warn('This ImageDataGenerator specifies ' '`samplewise_std_normalization`, ' 'which overrides setting of ' '`samplewise_center`.') if brightness_range is not None: if (not isinstance(brightness_range, (tuple, list)) or len(brightness_range) != 2): raise ValueError( '`brightness_range should be tuple or list of two floats. ' 'Received: %s' % (brightness_range,)) self.brightness_range = brightness_range 首先我们先看初始化部分从注释我们可以看出该类是用于以tensor的形式生成每一个batch，可以进行数据增强。注释部分和接口所示的参数基本一模一样，就不再说明。因为代码段太长了，相关的参数注释与相关示例选择性pass掉。在该部分中，很正常的读取参数并赋值，判断后端为tensorflow亦或是theano进行相应的对channel的操作接下来的便是这针对读取的参数进行相应的操作： zoom_range - 针对标量与范围矩阵进行赋值 zca_whitening - ZCA白化判断 featurewise_center、featurewise_std_normalization - 归一化操作判断 brightness_range - 略以上便是初始化中的内容，进行了简单的判断与赋值 flow部分1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465def flow(self, x, y=None, batch_size=32, shuffle=True, sample_weight=None, seed=None, save_to_dir=None, save_prefix='', save_format='png', subset=None): \"\"\"Takes data &amp; label arrays, generates batches of augmented data. # Arguments x: Input data. Numpy array of rank 4 or a tuple. If tuple, the first element should contain the images and the second element another numpy array or a list of numpy arrays that gets passed to the output without any modifications. Can be used to feed the model miscellaneous data along with the images. In case of grayscale data, the channels axis of the image array should have value 1, in case of RGB data, it should have value 3, and in case of RGBA data, it should have value 4. y: Labels. batch_size: Int (default: 32). shuffle: Boolean (default: True). sample_weight: Sample weights. seed: Int (default: None). save_to_dir: None or str (default: None). This allows you to optionally specify a directory to which to save the augmented pictures being generated (useful for visualizing what you are doing). save_prefix: Str (default: `''`). Prefix to use for filenames of saved pictures (only relevant if `save_to_dir` is set). save_format: one of \"png\", \"jpeg\" (only relevant if `save_to_dir` is set). Default: \"png\". subset: Subset of data (`\"training\"` or `\"validation\"`) if `validation_split` is set in `ImageDataGenerator`. # Returns An `Iterator` yielding tuples of `(x, y)` where `x` is a numpy array of image data (in the case of a single image input) or a list of numpy arrays (in the case with additional inputs) and `y` is a numpy array of corresponding labels. If 'sample_weight' is not None, the yielded tuples are of the form `(x, y, sample_weight)`. If `y` is None, only the numpy array `x` is returned. \"\"\" return NumpyArrayIterator( x, y, self, batch_size=batch_size, shuffle=shuffle, sample_weight=sample_weight, seed=seed, data_format=self.data_format, save_to_dir=save_to_dir, save_prefix=save_prefix, save_format=save_format, subset=subset ) 从注释可以看出flow函数用于处理raw-data以及label和根据图像增强的设置来生成batch其中 flow_from_directory部分123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105def flow_from_directory(self, directory, target_size=(256, 256), color_mode='rgb', classes=None, class_mode='categorical', batch_size=32, shuffle=True, seed=None, save_to_dir=None, save_prefix='', save_format='png', follow_links=False, subset=None, interpolation='nearest'): \"\"\"Takes the path to a directory &amp; generates batches of augmented data. # Arguments directory: string, path to the target directory. It should contain one subdirectory per class. Any PNG, JPG, BMP, PPM or TIF images inside each of the subdirectories directory tree will be included in the generator. See [this script]( https://gist.github.com/fchollet/0830affa1f7f19fd47b06d4cf89ed44d) for more details. target_size: Tuple of integers `(height, width)`, default: `(256, 256)`. The dimensions to which all images found will be resized. color_mode: One of \"grayscale\", \"rgb\", \"rgba\". Default: \"rgb\". Whether the images will be converted to have 1, 3, or 4 channels. classes: Optional list of class subdirectories (e.g. `['dogs', 'cats']`). Default: None. If not provided, the list of classes will be automatically inferred from the subdirectory names/structure under `directory`, where each subdirectory will be treated as a different class (and the order of the classes, which will map to the label indices, will be alphanumeric). The dictionary containing the mapping from class names to class indices can be obtained via the attribute `class_indices`. class_mode: One of \"categorical\", \"binary\", \"sparse\", \"input\", or None. Default: \"categorical\". Determines the type of label arrays that are returned: - \"categorical\" will be 2D one-hot encoded labels, - \"binary\" will be 1D binary labels, \"sparse\" will be 1D integer labels, - \"input\" will be images identical to input images (mainly used to work with autoencoders). - If None, no labels are returned (the generator will only yield batches of image data, which is useful to use with `model.predict_generator()`). Please note that in case of class_mode None, the data still needs to reside in a subdirectory of `directory` for it to work correctly. batch_size: Size of the batches of data (default: 32). shuffle: Whether to shuffle the data (default: True) If set to False, sorts the data in alphanumeric order. seed: Optional random seed for shuffling and transformations. save_to_dir: None or str (default: None). This allows you to optionally specify a directory to which to save the augmented pictures being generated (useful for visualizing what you are doing). save_prefix: Str. Prefix to use for filenames of saved pictures (only relevant if `save_to_dir` is set). save_format: One of \"png\", \"jpeg\" (only relevant if `save_to_dir` is set). Default: \"png\". follow_links: Whether to follow symlinks inside class subdirectories (default: False). subset: Subset of data (`\"training\"` or `\"validation\"`) if `validation_split` is set in `ImageDataGenerator`. interpolation: Interpolation method used to resample the image if the target size is different from that of the loaded image. Supported methods are `\"nearest\"`, `\"bilinear\"`, and `\"bicubic\"`. If PIL version 1.1.3 or newer is installed, `\"lanczos\"` is also supported. If PIL version 3.4.0 or newer is installed, `\"box\"` and `\"hamming\"` are also supported. By default, `\"nearest\"` is used. # Returns A `DirectoryIterator` yielding tuples of `(x, y)` where `x` is a numpy array containing a batch of images with shape `(batch_size, *target_size, channels)` and `y` is a numpy array of corresponding labels. \"\"\" return DirectoryIterator( directory, self, target_size=target_size, color_mode=color_mode, classes=classes, class_mode=class_mode, data_format=self.data_format, batch_size=batch_size, shuffle=shuffle, seed=seed, save_to_dir=save_to_dir, save_prefix=save_prefix, save_format=save_format, follow_links=follow_links, subset=subset, interpolation=interpolation ) 从注释可以看出flow_from_directory函数用于处理设定目录处的data和label根据图像增强的设置来生成batch其中的参数有： directory - 存放相关数据的目录 target_size - 同一将图片缩放至目标大小 color_mode - 图像的channel排布方式 class - 输入图像分类的类别序列，若为None则根据目录下子目录为类别序列 class_mode - 根据相关参数生成相应的labels batch_size - 一个batch的大小 shuffle - 是否打乱数据 seed - 用于生成打乱数据所需的随机数 save_to_dir - 用于储存图像增加（即该操作生成）的图片，若无需要不建议开启，运行一段时间后及其占储存空间 save_prefix、save_format - 上一项所需的关于储存图片的设置 follow_links - 略 subset - 将该目录图片分为训练集与验证集 interpolation - 缩放图片时采取的插值方式 validata_filenames - 略 最终返回的是一个赋值了相关参数的DirectoryIterator的结构接下来我们看一下DirectoryIterator的源码 DirectoryIterator部分123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899class DirectoryIterator(BatchFromFilesMixin, Iterator): \"\"\"Iterator capable of reading images from a directory on disk. # Arguments Pass \"\"\" allowed_class_modes = &#123;'categorical', 'binary', 'sparse', 'input', None&#125; def __init__(self, directory, image_data_generator, target_size=(256, 256), color_mode='rgb', classes=None, class_mode='categorical', batch_size=32, shuffle=True, seed=None, data_format='channels_last', save_to_dir=None, save_prefix='', save_format='png', follow_links=False, subset=None, interpolation='nearest', dtype='float32'): super(DirectoryIterator, self).set_processing_attrs(image_data_generator, target_size, color_mode, data_format, save_to_dir, save_prefix, save_format, subset, interpolation) self.directory = directory self.classes = classes if class_mode not in self.allowed_class_modes: raise ValueError('Invalid class_mode: &#123;&#125;; expected one of: &#123;&#125;' .format(class_mode, self.allowed_class_modes)) self.class_mode = class_mode self.dtype = dtype # First, count the number of samples and classes. self.samples = 0 if not classes: classes = [] for subdir in sorted(os.listdir(directory)): if os.path.isdir(os.path.join(directory, subdir)): classes.append(subdir) self.num_classes = len(classes) self.class_indices = dict(zip(classes, range(len(classes)))) pool = multiprocessing.pool.ThreadPool() # Second, build an index of the images # in the different class subfolders. results = [] self.filenames = [] i = 0 for dirpath in (os.path.join(directory, subdir) for subdir in classes): results.append( pool.apply_async(_list_valid_filenames_in_directory, (dirpath, self.white_list_formats, self.split, self.class_indices, follow_links))) classes_list = [] for res in results: classes, filenames = res.get() classes_list.append(classes) self.filenames += filenames self.samples = len(self.filenames) self.classes = np.zeros((self.samples,), dtype='int32') for classes in classes_list: self.classes[i:i + len(classes)] = classes i += len(classes) print('Found %d images belonging to %d classes.' % (self.samples, self.num_classes)) pool.close() pool.join() self._filepaths = [ os.path.join(self.directory, fname) for fname in self.filenames ] super(DirectoryIterator, self).__init__(self.samples, batch_size, shuffle, seed) @property def filepaths(self): return self._filepaths @property def labels(self): return self.classes @property # mixin needs this property to work def sample_weight(self): # no sample weights will be returned return None 从注释可以看出该类可以从目录中读取图片相关参数已经讲过便不再赘述从该代码段可知进行了生成类别序列的操作，匹配相应的路径最终返回一个结构返回了数据集中每张图片对应的路径以及序列方便后续的操作  flow_from_dataframe(self, dataframe, directory=None, x_col=\"filename\", y_col=\"class\", weight_col=None, target_size=(256, 256), color_mode='rgb', classes=None, class_mode='categorical', batch_size=32, shuffle=True, seed=None, save_to_dir=None, save_prefix='', save_format='png', subset=None, interpolation='nearest', validate_filenames=True, **kwargs): \"\"\"Takes the dataframe and the path to a directory and generates batches of augmented/normalized data. **A simple tutorial can be found **[here]( http://bit.ly/keras_flow_from_dataframe). # Arguments dataframe: Pandas dataframe containing the filepaths relative to `directory` (or absolute paths if `directory` is None) of the images in a string column. It should include other column/s depending on the `class_mode`: - if `class_mode` is `\"categorical\"` (default value) it must include the `y_col` column with the class/es of each image. Values in column can be string/list/tuple if a single class or list/tuple if multiple classes. - if `class_mode` is `\"binary\"` or `\"sparse\"` it must include the given `y_col` column with class values as strings. - if `class_mode` is `\"raw\"` or `\"multi_output\"` it should contain the columns specified in `y_col`. - if `class_mode` is `\"input\"` or `None` no extra column is needed. directory: string, path to the directory to read images from. If `None`, data in `x_col` column should be absolute paths. x_col: string, column in `dataframe` that contains the filenames (or absolute paths if `directory` is `None`). y_col: string or list, column/s in `dataframe` that has the target data. weight_col: string, column in `dataframe` that contains the sample weights. Default: `None`. target_size: tuple of integers `(height, width)`, default: `(256, 256)`. The dimensions to which all images found will be resized. color_mode: one of \"grayscale\", \"rgb\", \"rgba\". Default: \"rgb\". Whether the images will be converted to have 1 or 3 color channels. classes: optional list of classes (e.g. `['dogs', 'cats']`). Default: None. If not provided, the list of classes will be automatically inferred from the `y_col`, which will map to the label indices, will be alphanumeric). The dictionary containing the mapping from class names to class indices can be obtained via the attribute `class_indices`. class_mode: one of \"binary\", \"categorical\", \"input\", \"multi_output\", \"raw\", sparse\" or None. Default: \"categorical\". Mode for yielding the targets: - `\"binary\"`: 1D numpy array of binary labels, - `\"categorical\"`: 2D numpy array of one-hot encoded labels. Supports multi-label output. - `\"input\"`: images identical to input images (mainly used to work with autoencoders), - `\"multi_output\"`: list with the values of the different columns, - `\"raw\"`: numpy array of values in `y_col` column(s), - `\"sparse\"`: 1D numpy array of integer labels, - `None`, no targets are returned (the generator will only yield batches of image data, which is useful to use in `model.predict_generator()`). batch_size: size of the batches of data (default: 32). shuffle: whether to shuffle the data (default: True) seed: optional random seed for shuffling and transformations. save_to_dir: None or str (default: None). This allows you to optionally specify a directory to which to save the augmented pictures being generated (useful for visualizing what you are doing). save_prefix: str. Prefix to use for filenames of saved pictures (only relevant if `save_to_dir` is set). save_format: one of \"png\", \"jpeg\" (only relevant if `save_to_dir` is set). Default: \"png\". follow_links: whether to follow symlinks inside class subdirectories (default: False). subset: Subset of data (`\"training\"` or `\"validation\"`) if `validation_split` is set in `ImageDataGenerator`. interpolation: Interpolation method used to resample the image if the target size is different from that of the loaded image. Supported methods are `\"nearest\"`, `\"bilinear\"`, and `\"bicubic\"`. If PIL version 1.1.3 or newer is installed, `\"lanczos\"` is also supported. If PIL version 3.4.0 or newer is installed, `\"box\"` and `\"hamming\"` are also supported. By default, `\"nearest\"` is used. validate_filenames: Boolean, whether to validate image filenames in `x_col`. If `True`, invalid images will be ignored. Disabling this option can lead to speed-up in the execution of this function. Default: `True`. # Returns A `DataFrameIterator` yielding tuples of `(x, y)` where `x` is a numpy array containing a batch of images with shape `(batch_size, *target_size, channels)` and `y` is a numpy array of corresponding labels. \"\"\" if 'has_ext' in kwargs: warnings.warn('has_ext is deprecated, filenames in the dataframe have ' 'to match the exact filenames in disk.', DeprecationWarning) if 'sort' in kwargs: warnings.warn('sort is deprecated, batches will be created in the' 'same order than the filenames provided if shuffle' 'is set to False.', DeprecationWarning) if class_mode == 'other': warnings.warn('`class_mode` \"other\" is deprecated, please use ' '`class_mode` \"raw\".', DeprecationWarning) class_mode = 'raw' if 'drop_duplicates' in kwargs: warnings.warn('drop_duplicates is deprecated, you can drop duplicates ' 'by using the pandas.DataFrame.drop_duplicates method.', DeprecationWarning) return DataFrameIterator( dataframe, directory, self, x_col=x_col, y_col=y_col, weight_col=weight_col, target_size=target_size, color_mode=color_mode, classes=classes, class_mode=class_mode, data_format=self.data_format, batch_size=batch_size, shuffle=shuffle, seed=seed, save_to_dir=save_to_dir, save_prefix=save_prefix, save_format=save_format, subset=subset, interpolation=interpolation, validate_filenames=validate_filenames ) 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950def standardize(self, x): \"\"\"Applies the normalization configuration in-place to a batch of inputs. `x` is changed in-place since the function is mainly used internally to standarize images and feed them to your network. If a copy of `x` would be created instead it would have a significant performance cost. If you want to apply this method without changing the input in-place you can call the method creating a copy before: standarize(np.copy(x)) # Arguments x: Batch of inputs to be normalized. # Returns The inputs, normalized. \"\"\" if self.preprocessing_function: x = self.preprocessing_function(x) if self.rescale: x *= self.rescale if self.samplewise_center: x -= np.mean(x, keepdims=True) if self.samplewise_std_normalization: x /= (np.std(x, keepdims=True) + 1e-6) if self.featurewise_center: if self.mean is not None: x -= self.mean else: warnings.warn('This ImageDataGenerator specifies ' '`featurewise_center`, but it hasn\\'t ' 'been fit on any training data. Fit it ' 'first by calling `.fit(numpy_data)`.') if self.featurewise_std_normalization: if self.std is not None: x /= (self.std + 1e-6) else: warnings.warn('This ImageDataGenerator specifies ' '`featurewise_std_normalization`, ' 'but it hasn\\'t ' 'been fit on any training data. Fit it ' 'first by calling `.fit(numpy_data)`.') if self.zca_whitening: if self.principal_components is not None: flatx = np.reshape(x, (-1, np.prod(x.shape[-3:]))) whitex = np.dot(flatx, self.principal_components) x = np.reshape(whitex, x.shape) else: warnings.warn('This ImageDataGenerator specifies ' '`zca_whitening`, but it hasn\\'t ' 'been fit on any training data. Fit it ' 'first by calling `.fit(numpy_data)`.') return x 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687def get_random_transform(self, img_shape, seed=None): \"\"\"Generates random parameters for a transformation. # Arguments seed: Random seed. img_shape: Tuple of integers. Shape of the image that is transformed. # Returns A dictionary containing randomly chosen parameters describing the transformation. \"\"\" img_row_axis = self.row_axis - 1 img_col_axis = self.col_axis - 1 if seed is not None: np.random.seed(seed) if self.rotation_range: theta = np.random.uniform( -self.rotation_range, self.rotation_range) else: theta = 0 if self.height_shift_range: try: # 1-D array-like or int tx = np.random.choice(self.height_shift_range) tx *= np.random.choice([-1, 1]) except ValueError: # floating point tx = np.random.uniform(-self.height_shift_range, self.height_shift_range) if np.max(self.height_shift_range) &lt; 1: tx *= img_shape[img_row_axis] else: tx = 0 if self.width_shift_range: try: # 1-D array-like or int ty = np.random.choice(self.width_shift_range) ty *= np.random.choice([-1, 1]) except ValueError: # floating point ty = np.random.uniform(-self.width_shift_range, self.width_shift_range) if np.max(self.width_shift_range) &lt; 1: ty *= img_shape[img_col_axis] else: ty = 0 if self.shear_range: shear = np.random.uniform( -self.shear_range, self.shear_range) else: shear = 0 if self.zoom_range[0] == 1 and self.zoom_range[1] == 1: zx, zy = 1, 1 else: zx, zy = np.random.uniform( self.zoom_range[0], self.zoom_range[1], 2) flip_horizontal = (np.random.random() &lt; 0.5) * self.horizontal_flip flip_vertical = (np.random.random() &lt; 0.5) * self.vertical_flip channel_shift_intensity = None if self.channel_shift_range != 0: channel_shift_intensity = np.random.uniform(-self.channel_shift_range, self.channel_shift_range) brightness = None if self.brightness_range is not None: brightness = np.random.uniform(self.brightness_range[0], self.brightness_range[1]) transform_parameters = &#123;'theta': theta, 'tx': tx, 'ty': ty, 'shear': shear, 'zx': zx, 'zy': zy, 'flip_horizontal': flip_horizontal, 'flip_vertical': flip_vertical, 'channel_shift_intensity': channel_shift_intensity, 'brightness': brightness&#125; return transform_parameters 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354def apply_transform(self, x, transform_parameters): \"\"\"Applies a transformation to an image according to given parameters. # Arguments x: 3D tensor, single image. transform_parameters: Dictionary with string - parameter pairs describing the transformation. Currently, the following parameters from the dictionary are used: - `'theta'`: Float. Rotation angle in degrees. - `'tx'`: Float. Shift in the x direction. - `'ty'`: Float. Shift in the y direction. - `'shear'`: Float. Shear angle in degrees. - `'zx'`: Float. Zoom in the x direction. - `'zy'`: Float. Zoom in the y direction. - `'flip_horizontal'`: Boolean. Horizontal flip. - `'flip_vertical'`: Boolean. Vertical flip. - `'channel_shift_intencity'`: Float. Channel shift intensity. - `'brightness'`: Float. Brightness shift intensity. # Returns A transformed version of the input (same shape). \"\"\" # x is a single image, so it doesn't have image number at index 0 img_row_axis = self.row_axis - 1 img_col_axis = self.col_axis - 1 img_channel_axis = self.channel_axis - 1 x = apply_affine_transform(x, transform_parameters.get('theta', 0), transform_parameters.get('tx', 0), transform_parameters.get('ty', 0), transform_parameters.get('shear', 0), transform_parameters.get('zx', 1), transform_parameters.get('zy', 1), row_axis=img_row_axis, col_axis=img_col_axis, channel_axis=img_channel_axis, fill_mode=self.fill_mode, cval=self.cval, order=self.interpolation_order) if transform_parameters.get('channel_shift_intensity') is not None: x = apply_channel_shift(x, transform_parameters['channel_shift_intensity'], img_channel_axis) if transform_parameters.get('flip_horizontal', False): x = flip_axis(x, img_col_axis) if transform_parameters.get('flip_vertical', False): x = flip_axis(x, img_row_axis) if transform_parameters.get('brightness') is not None: x = apply_brightness_shift(x, transform_parameters['brightness']) return x 12345678910def random_transform(self, x, seed=None): \"\"\"Applies a random transformation to an image. # Arguments x: 3D tensor, single image. seed: Random seed. # Returns A randomly transformed version of the input (same shape). \"\"\" params = self.get_random_transform(x.shape, seed) return self.apply_transform(x, params) 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475def fit(self, x, augment=False, rounds=1, seed=None): \"\"\"Fits the data generator to some sample data. This computes the internal data stats related to the data-dependent transformations, based on an array of sample data. Only required if `featurewise_center` or `featurewise_std_normalization` or `zca_whitening` are set to True. # Arguments x: Sample data. Should have rank 4. In case of grayscale data, the channels axis should have value 1, in case of RGB data, it should have value 3, and in case of RGBA data, it should have value 4. augment: Boolean (default: False). Whether to fit on randomly augmented samples. rounds: Int (default: 1). If using data augmentation (`augment=True`), this is how many augmentation passes over the data to use. seed: Int (default: None). Random seed. \"\"\" x = np.asarray(x, dtype=self.dtype) if x.ndim != 4: raise ValueError('Input to `.fit()` should have rank 4. ' 'Got array with shape: ' + str(x.shape)) if x.shape[self.channel_axis] not in &#123;1, 3, 4&#125;: warnings.warn( 'Expected input to be images (as Numpy array) ' 'following the data format convention \"' + self.data_format + '\" (channels on axis ' + str(self.channel_axis) + '), i.e. expected ' 'either 1, 3 or 4 channels on axis ' + str(self.channel_axis) + '. ' 'However, it was passed an array with shape ' + str(x.shape) + ' (' + str(x.shape[self.channel_axis]) + ' channels).') if seed is not None: np.random.seed(seed) x = np.copy(x) if augment: ax = np.zeros( tuple([rounds * x.shape[0]] + list(x.shape)[1:]), dtype=self.dtype) for r in range(rounds): for i in range(x.shape[0]): ax[i + r * x.shape[0]] = self.random_transform(x[i]) x = ax if self.featurewise_center: self.mean = np.mean(x, axis=(0, self.row_axis, self.col_axis)) broadcast_shape = [1, 1, 1] broadcast_shape[self.channel_axis - 1] = x.shape[self.channel_axis] self.mean = np.reshape(self.mean, broadcast_shape) x -= self.mean if self.featurewise_std_normalization: self.std = np.std(x, axis=(0, self.row_axis, self.col_axis)) broadcast_shape = [1, 1, 1] broadcast_shape[self.channel_axis - 1] = x.shape[self.channel_axis] self.std = np.reshape(self.std, broadcast_shape) x /= (self.std + 1e-6) if self.zca_whitening: if scipy is None: raise ImportError('Using zca_whitening requires SciPy. ' 'Install SciPy.') flat_x = np.reshape( x, (x.shape[0], x.shape[1] * x.shape[2] * x.shape[3])) sigma = np.dot(flat_x.T, flat_x) / flat_x.shape[0] u, s, _ = linalg.svd(sigma) s_inv = 1. / np.sqrt(s[np.newaxis] + self.zca_epsilon) self.principal_components = (u * s_inv).dot(u.T)","categories":[{"name":"源码阅读","slug":"源码阅读","permalink":"https://lzhnb.github.io/categories/源码阅读/"}],"tags":[{"name":"Keras","slug":"Keras","permalink":"https://lzhnb.github.io/tags/Keras/"},{"name":"ImageDataGenerator","slug":"ImageDataGenerator","permalink":"https://lzhnb.github.io/tags/ImageDataGenerator/"},{"name":"数据生成","slug":"数据生成","permalink":"https://lzhnb.github.io/tags/数据生成/"}]},{"title":"Prototypical_Networks_for_Few-shot_Learning论文解读","slug":"Prototypical-Networks","date":"2019-04-27T15:01:37.000Z","updated":"2019-05-01T09:46:58.775Z","comments":true,"path":"category/2019/04/27/Prototypical-Networks.html","link":"","permalink":"https://lzhnb.github.io/category/2019/04/27/Prototypical-Networks.html","excerpt":"","text":"Prototypical Network论文解读（施工中）关于小样本学习详情见Matching_Networks_for_One_Shot_Learning论文解读 网络结构 图示：","categories":[{"name":"论文笔记","slug":"论文笔记","permalink":"https://lzhnb.github.io/categories/论文笔记/"}],"tags":[{"name":"小样本学习","slug":"小样本学习","permalink":"https://lzhnb.github.io/tags/小样本学习/"},{"name":"Prototypical-Network","slug":"Prototypical-Network","permalink":"https://lzhnb.github.io/tags/Prototypical-Network/"}]},{"title":"Few-Shot-Semantic-Segmentation-with-Prototype-Learning论文解读","slug":"Few-Shot-Semantic-Segmentation-with-Prototype-Learning论文解读","date":"2019-04-25T15:28:43.000Z","updated":"2019-04-28T13:49:09.650Z","comments":true,"path":"category/2019/04/25/Few-Shot-Semantic-Segmentation-with-Prototype-Learning论文解读.html","link":"","permalink":"https://lzhnb.github.io/category/2019/04/25/Few-Shot-Semantic-Segmentation-with-Prototype-Learning论文解读.html","excerpt":"","text":"Few-Shot-Semantic-Segmentation-with-Prototype-Learning论文解读（施工中）关于小样本学习详情见Matching_Networks_for_One_Shot_Learning论文解读","categories":[{"name":"论文笔记","slug":"论文笔记","permalink":"https://lzhnb.github.io/categories/论文笔记/"}],"tags":[{"name":"小样本学习","slug":"小样本学习","permalink":"https://lzhnb.github.io/tags/小样本学习/"},{"name":"Semantic-Segmentation","slug":"Semantic-Segmentation","permalink":"https://lzhnb.github.io/tags/Semantic-Segmentation/"},{"name":"Prototypical-Network","slug":"Prototypical-Network","permalink":"https://lzhnb.github.io/tags/Prototypical-Network/"}]},{"title":"Matching_Networks_for_One_Shot_Learning论文解读","slug":"Matching-Networks-for-One-Shot-Learning论文解读","date":"2019-04-25T15:12:09.000Z","updated":"2019-05-01T09:49:36.854Z","comments":true,"path":"category/2019/04/25/Matching-Networks-for-One-Shot-Learning论文解读.html","link":"","permalink":"https://lzhnb.github.io/category/2019/04/25/Matching-Networks-for-One-Shot-Learning论文解读.html","excerpt":"","text":"Matching_Networks_for_One_Shot_Learning论文解读（施工中）小样本学习(Few-shot Learning)深度学习所遇到问题在如今深度学习已经发展到一定的地步了，在计算机视觉的很多领域取得了很好的效果，但是很多方法都需要针对特定任务采集大量的数据作为训练数据集才能使得网络收敛到应有的效果。 如何减少训练过程中所需的数据集，或者说高效地利用数量较少的数据集一直以来都是相关从业人员们所重视的问题。 但是随着网络结构的逐渐加深，哪怕是有相当好的网络训练技巧也很难避免过拟合的出现，或者说解决数据集过小的问题。 解决方案神经网络初衷便是模拟人脑的结构，模拟神经元之间信息传递的方式而搭建的数学模型。 以一个人的学习经验来看，并不是一个强监督的过程，在积累了一定程度的先验知识后，对于新的事物仅需要个例便可以马上接受该事务，识别出该事物。 例如：一个在上小学的小孩子在学校里经过老师的介绍仅从图片上亦或是文字描述了解了长颈鹿，但一去动物园他就能马上认出长颈鹿；但是，这对于一个刚出生的婴儿来说基本是远远不能做到的。从以上的例子，相关的从业人员便提出了小样本学习(Few-shot Learning)的概念。 小样本学习简介小样本学习并不是字面上利用已有的少量数据集通过传统的fine-tuning，只要接触过深度学习的都知道无论在预处理或是网络加入了何种结构或是方法，仅有不超过白张十张的数据集进行fine-tuning时，毫无疑问会出现严重的过拟合。 所以小样本学习实际上是搭建一个网络能够接受多个输入，在训练过程中依旧是需要大数据量进行训练，但是在测试或是实际使用过程中，我们提供一个输入样本-support_set，以及代处理输入-query_input，来输出我们依据support_set输出query_input相应的结果。 就如我们训练一个分类网络识别长颈鹿，过往的方法便是我们收集大量的长颈鹿的图片作为训练集来训练这个分类网络，在测试过程中就利用长颈鹿以及非长颈鹿的图片来测试该网络的性能。 若用小样本学习的方法便是，我们训练好一个网络，然后进行测试，在测试过程中： 首先我们将确定是长颈鹿的图片作为该网络的一个输入-support_set，而后我们将测试的图片作为该网络的另一个输入-query_input，最后该网络输出的便是关于这个query_input是不是长颈鹿的分类结果，其中作为输入的support_set可以接受不同数量的输入，便是k-shot的直观解释（单输入便是one-shot）这就是小样本学习的比较直观的概念 要注意的是，在上面这个过程中，在训练的时候长颈鹿是不能作为训练集参与训练的！仅在测试集中进行测试使用，所以小样本学习的目标不参与该网络的训练，这也表现了小样本学习的一个较强的泛化能力以及普适性，不过很显然准确性是小样本学习遇到的相当大地问题。 所以我们能总结出小样本学习的一下特征点： 能够接受多输入，但输入总体就分为提供相关任务信息的support_set以及代处理的query_input 测试集（的种类）不应该出现在训练集（的种类）中 Matching Network介绍来源针对上面所说的小样本学习的相关概念，google DeepMind实验室在2016年发表了这篇文章Matching Networks for One Shot Learning，这篇文章针对小样本分类问题提出了一个非常直观且有效的网络框架，并且提供了相应的训练策略和损失函数的选取。 网络结构 图示： 解读：结合上图以及原文信息来看，首先是输入 Support_set - $S$，$S=\\lbrace (x_i, y_i) \\rbrace_{i=1}^{k}$，其中$k$表示总个数，并不指类别数或者是单个类别的输入样本数。$x_i$ 指的是输入样本， $y_i$ 指的是对应输入样本的label。紧接在我们输入query_input-$\\hat{x}$ ，对应的 pridiction 为 $\\hat{y}$-包含了该图片对应各个类别的分类”得分”，对应过程我们可以写成： S\\rightarrow{c_S(\\hat{x})}也可以写成: P(\\hat{y}|\\hat{x},\\ S)当 Support_set 发生改变变为 $S’$ 时，对应网络的参数与结构并没有改变，对应输出的 $\\hat{y}’$ (论文中并不是这种表达形式，仅为我用此做说明)则对应：$\\ P(\\hat{y}|\\hat{x},\\ S’)$ 注意力机制那么如何计算各个类别的得分呢，对应该模型，论文给出如下关系式： P(\\hat{y}|\\hat{x},S)\\ =\\ \\sum_{i=1}^{k}a(\\hat{x},x_i)y_i很多参数在上面就已经说明，这里出现了一个新的定义-$a$，原文中将其定义为该模型的一个注意力机制- attention_mechanism。从上面式子我们可以看出对于 query_input-$\\hat{x}$ 的”得分”-$\\hat{y}$是由Support_set对应的”得分”- $y_i$线性组合而成的。将该模型简化来看的话就是： $x_i$ 与 $\\hat{x}$ 可以看作 $X1$ 的特征向量$a$ 可以看作是一个Kernel尺寸为 $XX$ 的核密度估计器-kernel density estimator 所以这样我们就可以通过$x_i$与$\\hat{x}$之间的匹配程度评估”得分”，这种工作方式类似与最近邻网络- KNN。 该模型的关键便是在于对 $a$ 的定义，在论文中对$a$进行了一种比较简单且有效的定义： a(\\hat{x}, x_i)\\ =\\ e^{c(f(\\hat{x}),g(x_i))}/\\sum_{j=1}^{k}e^{c(f(\\hat{x}),g(x_j))}其中，$f$与$g$分别对应着网络结构图示的$f_\\theta$与$g_\\theta$，分别用于生成support_set中$x_i$与query_input的$\\hat{x}$的特征向量，也可以看作这些函数是将输入的图片映射到高维的特征空间(embedding)。这两个函数可以看作两个神经网络，论文中说明用的是 VGG 与 Inception模型，两个网络也可以相同甚至共享参数(siamese network)。 匹配（Matching）机制对于得到的这一组特征向量，下面就进入了比较关键的环节也涉及到了这个网络的核心部分，就说标题写的Matching机制。那如何进行matching呢，从公式中我们可以看到 $e^{c(f(\\hat{x}),g(x_i))}$ 涉及到了 $f(\\hat{x})$ 和 $g(x_i)$ ，这一步是计算两个向量间的 余弦距离（相似性） ，余弦距离公式如下： 对于已有的两个向量$a$与$b$:它们的余弦距离如下： cos\\theta = \\frac{A \\cdot B}{||A||\\ ||B||} = \\frac{A_1*B_1+ ... + A_n*B_n}{\\sqrt{A_1^2+...A_n^2}+\\sqrt{B_1^2+...+B_n^2}}=\\frac{\\sum^{n}_{i=1}A_i*B_i}{\\sqrt{\\sum^{n}_{i=1}(A_i^2)}+\\sqrt{\\sum^{n}_{i=1}}(B_i^2)}和欧式距离相比，欧式距离更重视两个特征向量间空间距离，而余弦距离更重视两个特征向量在方向上的匹配性。 最后调用softmax函数来得到query_input关于support_set中各个图片所属类别的权重，进行线性叠加。这种做法类似于度量学习- metric learning ，利用空间距离来度量特征间的相似度。论文中也强调了，这个模型在于解决 $N-way,1-shot$ 的分类问题。 embedding不同于正常分类问题的 one-hot 编码机制，这种方法采用 embedding 的方法，其实这种方法更常用于NLP等领域，通过神经网络将图像映射到抽象的高维特征空间中的特征向量，类似于度量学习中比较向量间的相似度来进行分类。所以在这种方法中网络对图片的映射结果就十分重要，就拿论文中的结构来说：假设该网络可以接受$k-shot$的输入，一个好的网络，相同类别的图片（假设$x_i$与$x_j$）之间我们希望它们之间的余弦相似度相当高，而不同类别的图片（假设$x_i$与$x_k$）之间的余弦相似度并不高。而且这里如果采用两个网络的话，尽管网络并不相同，但是我们依旧希望它们映射的 embedding 尽量相近，事实证明这是可行的，而且采用两种网络使得图片映射到的 embedding 相近可以用于很多别的用途，比如能够使得网络两端接受不同大小的输入等。 其他对于如何利用 $S$ 和使得整个网络映射到 embedding 的效果更好，论文中采用了 LSTM ，不过本人对 LSTM 并不熟悉，这一部分不加以赘述，论文中公式如下： \\hat{h}_k, c_k\\ =\\ LSTM(f'(\\hat{x}),[h_{k-1}, r_{k-1}],c_{k-1})h_k\\ =\\ \\hat{h}_k +f'(\\hat{x})r_{k-1}\\ = \\ \\sum_{i=1}^{|S|}a(h_{k-1},g(x_i))g(x_i)a(h_{k-1}, g(x_i))\\ =\\ e^{h^{T_{k-1}}g(x_i)}/\\sum_{j=1}^{|s|}e^{h^{t}_{k-1}g(x_j)}训练策略(Training Strategy)小样本学习是一种创新性的方法，但它依旧是以神经网络作为实现的基础，和传统神经网络的训练有所不同，虽然依旧是强监督式的训练方法，但是不同于以往的单输入单输出与label进行比对的方法，小样本学习的输入并不是单张的图片，或者说其输入是一个又一个的 pair ，而在同样数量的数据集下，这种 pair 形式的组合有着很多种组合方式，可以拓宽训练集的数量。但是如何组合 pair ，怎么定义损失函数也是一门学问。这篇论文有个很重要的意义便是提出了一种非常完整且有效的训练策略。首先之前定义了 prediction 的过程： P(\\hat{y}|\\hat{x},\\ S)现在我们暂时不考虑输出$\\hat{y}$，同时加入神经网络的参数- $\\theta$: P_\\theta(.|\\hat{x},\\ S)首先在训练过程中，针对任务集- $T$ 我们从中采样一批数据集- $L$，这些数据集包括了输入以及对应的label，在这之后我们从采样集- $L$ 中挑选某个类别中的某些图片作为 support_set- $S$， 再取样一批图片作为 query_input- $B$，这样作为一个batch投入训练。例如: 我们从总共有 “猫，狗，鼠，牛，羊” 五类的数据集- $T$ 从中挑选其中两类:”猫，狗”作为采样集（训练集）- $L$ 然后我们从采样类-$L$ 中挑选图片生成 $episode$，可以看成传统训练中的 batch。 首先我们针对每个类别选取 $k$ 张图片作为 support_set，然后选取 batch（指数量） 张图片作为 query_input，每次训练将 support_set 与 query_input 中的一张图片形成 pair 作为训练的输入 训练完后将数据集- $T$ 除去采样集 $L$ 中种类的剩下种类”鼠，牛，羊”的数据集中生成 pair 作为验证集输入进行验证 重点：训练集与验证集之间的种类不应该有重叠，假设验证集为 $V$，则有: cls_V \\cap cls_L=\\emptyset 那么在训练过程中要使模型收敛我们需要定义其约束函数，如下： \\theta = argmax_\\theta E_{L∼ T}[E_{S∼ L,B∼ L}[\\sum_{(x, y)\\in{B}}log\\ P_\\theta (y|x, S)]]所以整个训练过程就是为了使预测值 Pridiction- $P_\\theta (y|x, S)$ 与 ground_truth 的匹配度尽可能的高。 \\theta = argmax_\\theta E_{L∼ T}[E_{S∼ L,B∼ L}[\\sum_{(x, y)\\in{B}}log\\ P_\\theta (y|x, S)]]而且在不同的 $episode-S’∼ L’$ 以及不同的数据集- $T’$ 上的性能表现尽可能的高","categories":[{"name":"论文笔记","slug":"论文笔记","permalink":"https://lzhnb.github.io/categories/论文笔记/"}],"tags":[{"name":"小样本学习","slug":"小样本学习","permalink":"https://lzhnb.github.io/tags/小样本学习/"},{"name":"Matching-Network","slug":"Matching-Network","permalink":"https://lzhnb.github.io/tags/Matching-Network/"}]},{"title":"查看空闲gpu并运行程序","slug":"查看空闲gpu并运行程序","date":"2019-04-25T09:12:47.000Z","updated":"2019-05-01T09:50:55.817Z","comments":true,"path":"category/2019/04/25/查看空闲gpu并运行程序.html","link":"","permalink":"https://lzhnb.github.io/category/2019/04/25/查看空闲gpu并运行程序.html","excerpt":"","text":"查看空闲gpu并运行程序（施工中）","categories":[{"name":"深度学习","slug":"深度学习","permalink":"https://lzhnb.github.io/categories/深度学习/"}],"tags":[{"name":"代码技巧","slug":"代码技巧","permalink":"https://lzhnb.github.io/tags/代码技巧/"}]},{"title":"介绍","slug":"介绍","date":"2019-04-24T11:49:27.000Z","updated":"2019-04-28T13:49:41.003Z","comments":true,"path":"category/2019/04/24/介绍.html","link":"","permalink":"https://lzhnb.github.io/category/2019/04/24/介绍.html","excerpt":"","text":"开启博客之路！我的第一篇博客就这样开始啦！利用博客记录我的码农生活1print (&quot;hello world!&quot;)","categories":[{"name":"生活","slug":"生活","permalink":"https://lzhnb.github.io/categories/生活/"}],"tags":[{"name":"生活","slug":"生活","permalink":"https://lzhnb.github.io/tags/生活/"}]}]}